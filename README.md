# UUID NFC

A small and lightweight Android app that exclusively writes and reads `text/plain` data (such as UUID strings) from re-writable NFC tags.

## Install

Precompiled APKs available via the [Releases](https://github.com/jack-margeson/uuid-nfc/releases) page. Otherwise, clone the repository and build it yourself using Gradle.
